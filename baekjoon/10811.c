#include <stdio.h>
int main(){
    int n,m,a,b;
    scanf("%d %d",&n,&m);
    int arr[n];
    for(int i=1;i<6;i++){
        arr[i]=i;
    }
    for(int i=0;i<m;i++){
        scanf("%d %d\n",&a,&b);
        for(int i=a-1;i<b;)
    }
    return 0;
}